import { Metadata } from 'next';

// We need to extract metadata to a separate file since our layout is now a client component
export const metadata: Metadata = {
  title: 'Autenticación | Andalucía Activa',
  description: 'Inicia sesión o regístrate en Andalucía Activa para acceder a todas las funcionalidades',
};

