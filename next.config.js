/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Si se necesitan otras configuraciones de Next.js, se pueden añadir aquí.
  // Por ejemplo, para configurar el compilador de SWC para Tailwind CSS si es necesario,
  // o para configurar experimental.appDir = true (aunque esto es por defecto ahora).
};

export default nextConfig;

